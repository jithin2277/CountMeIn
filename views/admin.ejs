<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Count Me In - Admin</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css" integrity="sha384-9gVQ4dYFwwWSjIDZnLEWnxCjeSWFphJiwGPXr1jddIhOegiu1FwO5qRGvFXOdJZ4"
        crossorigin="anonymous">
    <style type="text/css">
        .input-group {
            margin-bottom: 20px;
        }
    </style>
</head>

<body class="bg-light">
    <div class="container" style="margin-top: 40px;" id="admin">
        <div class="row">
            <div class="col-md-8 order-md-2 mb-4">
                <div class="input-group">
                    <input type="password" v-model="magicWord" class="form-control" placeholder="Magic Word to reset bookings!">
                    <div class="input-group-append">
                        <button type="submit" class="btn btn-secondary" v-on:click="resetBookings">Reset</button>
                    </div>
                </div>
                <div class="input-group">
                    <input type="password" v-model="manageCode" class="form-control" placeholder="Code to start/stop bookings!">
                    <div class="input-group-append">
                        <button type="submit" class="btn btn-info" v-on:click="manageBookings">Start/Stop</button>
                    </div>
                </div>
                <h4 class="d-flex justify-content-between align-items-center mb-3">
                    <span class="text-muted">Players</span>
                    <span class="badge badge-secondary badge-pill">{{ playerList.length }}</span>
                </h4>
                <ul class="list-group mb-3">
                    <li class="list-group-item d-flex justify-content-between lh-condensed" v-for="player in playerList">
                        <div>
                            <h6 class="my-0">{{ player }}</h6>
                        </div>
                    </li>
                </ul>
            </div>
        </div>

        <footer class="my-5 pt-5 text-muted text-center text-small">
            <p class="mb-1">&copy; 2017-2018 Company Name</p>
            <ul class="list-inline">
                <li class="list-inline-item">
                    <a href="#">Privacy</a>
                </li>
                <li class="list-inline-item">
                    <a href="#">Terms</a>
                </li>
                <li class="list-inline-item">
                    <a href="#">Support</a>
                </li>
            </ul>
        </footer>
    </div>
</body>

<script src="https://code.jquery.com/jquery-1.12.4.js"></script>
<script src="https://cdn.jsdelivr.net/npm/vue"></script>
<script src="/socket.io/socket.io.js"></script>
<script src="/js/admin.js"></script>

</html>